## How to Run

- Make sure all input test data is in the same directory as `chain-code-boundary-analysis.cpp` before compiling and running.

1) g++ -std=c++11  chain-code-boundary-analysis.cpp -o chain-code-analysis
2) ./chain-code-analysis img2.txt img2Property.txt outFile.txt chainCodeFile.txt boundary.txt logFile.txt
