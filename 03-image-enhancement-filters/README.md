## How to Run

- Make sure all input test data is in the same directory as `image-enhancement-filters.cpp` before compiling and running.

1) g++ -std=c++11 image-enhancement-filters.cpp -o image-enhancement-filters
2) ./image-enhancement-filters img1.txt mask.txt 22 avg.txt median.txt gauss.txt log.txt
