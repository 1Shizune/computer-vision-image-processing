## How to Run

- Make sure all input test data is in the same directory as the src folder before compiling and running.

1) g++ -std=c++11 image-thresholding-histogram.cpp -o image-thresholding-histogram
2) ./image-thresholding-histogram img2.txt 25 count.txt graph.txt output.txt logFile.txt
